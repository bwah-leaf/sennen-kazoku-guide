---
layout: page
title: 조사 및 검증 방법
permalink: /research-method/
---

# 조사 및 검증 방법

이 위키는 제목이나 대사의 분위기만으로 발생 조건을 단정하지 않습니다. 각 정보는
다음의 증거 상태를 가집니다.

| 표시 | 뜻 |
|---|---|
| 미확인 | 아직 조건을 조사하지 않음 |
| 문맥 추정 | 제목·대사에서 추정했지만 코드와 실기로 확인하지 않음 |
| 정적 확인 | ROM의 테이블·분기·조건 검사 코드를 확인함 |
| 구조 확인 | 어느 발생 시스템과 항목에 연결되는지는 확인했으나 그 시스템의 외부 조건은 아직 분리 중 |
| 실기 확인 | 에뮬레이터에서 조건을 재현함 |
| 결과 충돌 | 정적 분석과 실행 결과가 맞지 않아 재조사가 필요함 |

## 공략 항목에 기록할 내용

발생 시기, 필요한 인물 상태, 능력치·직업·관계 조건, 확률 또는 우선순위, 이벤트가
바꾸는 수치와 후속 이벤트를 분리해서 적습니다. 확인에 사용한 세이브나 스크린샷은
파일명과 함께 남기되 ROM과 상태저장 파일 자체는 공개 저장소에 올리지 않습니다.

## ROM 정적 발생 조건 표시

일반 이벤트는 5개 발생 그룹의 1,109개 상위 판정 레코드와 그 아래의 후보 목록으로
연결됩니다. 전용 시스템과 하위 결과까지 합쳐 실제 경로에서 쓰이는 고유 Thumb 함수
7,863개를 분기 경로별로 해석해 AND·OR 조건, 연령, 날짜, 관계·상태 코드와 난수 확률을
표시합니다. 한 이벤트가 여러 상위 레코드에서 공용 후보로 쓰이면 조건을 하나로
합치지 않고 **서로 다른 발생 경로**로 나눠 보여 줍니다.

`인물 속성 #27`은 접근기와 화면 나이 사용 경로를 대조해 연령으로 판독했습니다.
아직 의미가 확정되지 않은 속성과 전역 상태는 임의의 이름을 붙이지 않고 번호를
보존합니다. `항상 참`도 해당 판정 함수 안에 추가 제한이 없다는 뜻이며, 게임의 하루
진행·그룹 우선순위까지 무시하고 언제든 발생한다는 뜻은 아닙니다.

사고·인생·악마·꿈·인간관계 전용 표 2,270개도 텍스트 포인터의 ROM 역참조를 따라
후보 항목, 후보표, 0x28바이트 발생 레코드와 각 단계의 판정 함수를 복원했습니다.
2,192개는 실제 경로가 확인되어 **정적 확인**으로 표시합니다. 여기에 같은 구조로
연결된 하위 결과 16개를 더해 전체 5,393개 중 5,312개가 정적 확인 상태입니다.

구조 확인으로 남은 81개는 조건 누락으로 한데 묶지 않습니다. 70개는 장면·배경 선택
자산, 8개는 발생 후보표에서 참조되지 않는 미사용 레코드, 3개는 직접 결과 풀 소속만
확인된 하위 결과입니다. 디버그 이벤트 체커의 선택 코드와 사건 메모 표시 코드는
실제 발생 조건에서 제외했습니다.

## 번역 데이터와의 관계

각 이벤트에는 제목 UID와 스크립트 UID가 표시됩니다. 이는 공략의 근거가 어느 번역
항목인지 찾기 위한 값입니다. 조사 마커나 제어코드는 공략 데이터에서 고치지 않고
번역 작업공간에서 수정해야 합니다.
